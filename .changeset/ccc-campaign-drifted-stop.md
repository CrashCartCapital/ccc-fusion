---
"@runfusion/fusion": patch
---

summary: A campaign whose stored manifest no longer reconstructs can be closed with fn prd stop-drifted, which preserves worktrees, branches, and approvals and stops the import re-projecting its task directories.
category: fix
