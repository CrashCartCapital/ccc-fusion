export const gate2TelemetryContract = Object.freeze({
  projectName: "gate2-telemetry-service",
  prdFileName: "PRJ-HUM-Gate2TelemetryService-PRD-v1.0.0.md",
  supportRelativePath: "support/REF-HUM-Gate2TelemetryVerifier.md",
  workflowId: "WORKFLOW-TELEMETRY",
  integratedProofId: "PROOF-TELEMETRY-INTEGRATED",
  fixturePaths: Object.freeze(["fixtures/events.json"]),
});

export const gate2TelemetryTaskSpecs = Object.freeze([
  {
    requirementId: "REQ-TELEMETRY-CONTRACT", taskId: "TASK-TELEMETRY-CONTRACT",
    proofId: "PROOF-TELEMETRY-CONTRACT", clauseId: "AC-REQ-TELEMETRY-CONTRACT-001",
    title: "Define the telemetry event contract",
    statement: "In src/contract.ts, export a strict TelemetryEvent type and parseTelemetryEvent(value: unknown).",
    acceptance: "parseTelemetryEvent(value: unknown) preserves valid declared fields and rejects malformed, incomplete, and unknown-field events.",
    command: "task verify:contract",
    positiveOracle: "Valid telemetry events are parsed without losing declared fields.",
    negativeControl: "Malformed, missing-field, and unknown-field events are rejected.",
    ownedPaths: ["src/contract.ts"],
    dependencyTaskIds: [], dependencyRequirementIds: [], routeKey: "glm",
  },
  {
    requirementId: "REQ-TELEMETRY-INGEST", taskId: "TASK-TELEMETRY-INGEST",
    proofId: "PROOF-TELEMETRY-INGEST", clauseId: "AC-REQ-TELEMETRY-INGEST-001",
    title: "Implement HTTP event ingest",
    statement: "In src/ingest.ts, export handleIngest(value, dependencies) against the frozen event contract; dependencies provide audit.append(event) and broadcast.publish(event).",
    acceptance: "handleIngest(value, dependencies) returns a stable { status, body } result: valid events succeed, invalid events return a 4xx status, and rejected events are neither persisted nor broadcast.",
    command: "task verify:ingest",
    positiveOracle: "A valid event POST is accepted with a stable response body.",
    negativeControl: "An invalid event POST returns 4xx and creates no accepted event.",
    ownedPaths: ["src/ingest.ts"],
    dependencyTaskIds: ["TASK-TELEMETRY-CONTRACT"],
    dependencyRequirementIds: ["REQ-TELEMETRY-CONTRACT"], routeKey: "minimax",
  },
  {
    requirementId: "REQ-TELEMETRY-AUDIT", taskId: "TASK-TELEMETRY-AUDIT",
    proofId: "PROOF-TELEMETRY-AUDIT", clauseId: "AC-REQ-TELEMETRY-AUDIT-001",
    title: "Implement append-only audit storage",
    statement: "In src/audit.ts, export AuditStore with AuditStore.open(filePath), append(event), and readAll().",
    acceptance: "AuditStore.open(filePath) provides append-only canonical JSONL persistence; append(event) records each accepted identifier once and readAll() returns the records after reopen.",
    command: "task verify:audit",
    positiveOracle: "Accepted events survive restart in canonical JSONL form.",
    negativeControl: "Duplicate append attempts do not create duplicate accepted records.",
    ownedPaths: ["src/audit.ts"],
    dependencyTaskIds: ["TASK-TELEMETRY-CONTRACT"],
    dependencyRequirementIds: ["REQ-TELEMETRY-CONTRACT"], routeKey: "glm",
  },
  {
    requirementId: "REQ-TELEMETRY-BROADCAST", taskId: "TASK-TELEMETRY-BROADCAST",
    proofId: "PROOF-TELEMETRY-BROADCAST", clauseId: "AC-REQ-TELEMETRY-BROADCAST-001",
    title: "Implement SSE event broadcast",
    statement: "In src/broadcast.ts, export SseHub with publish(event) and subscribe(), where subscribe() returns an AsyncIterable<string> of SSE frames.",
    acceptance: "A subscribe() consumer receives each accepted event as an SSE frame within the bounded proof timeout, while rejected events are not published.",
    command: "task verify:broadcast",
    positiveOracle: "A subscriber receives the accepted event with its canonical identifier.",
    negativeControl: "Rejected events are not broadcast to subscribers.",
    ownedPaths: ["src/broadcast.ts"],
    dependencyTaskIds: ["TASK-TELEMETRY-CONTRACT"],
    dependencyRequirementIds: ["REQ-TELEMETRY-CONTRACT"], routeKey: "gemini",
  },
  {
    requirementId: "REQ-TELEMETRY-CLI", taskId: "TASK-TELEMETRY-CLI",
    proofId: "PROOF-TELEMETRY-CLI", clauseId: "AC-REQ-TELEMETRY-CLI-001",
    title: "Implement the health probe and operator guide",
    statement: "In src/health-cli.ts, export probeHealth(fetchFn, baseUrl) and keep the executable entry behind an explicit import.meta.url main guard; provide concise operator documentation.",
    acceptance: "probeHealth(fetchFn, baseUrl) returns zero for a healthy response and nonzero for an unavailable response; importing the module has no external effect, and the guide provides executable operation and verification steps.",
    command: "task verify:cli",
    positiveOracle: "The health probe succeeds against the running service and the guide covers normal operation.",
    negativeControl: "The health probe fails nonzero when the service is unavailable.",
    ownedPaths: ["src/health-cli.ts", "README.md"],
    dependencyTaskIds: ["TASK-TELEMETRY-INGEST", "TASK-TELEMETRY-AUDIT", "TASK-TELEMETRY-BROADCAST"],
    dependencyRequirementIds: ["REQ-TELEMETRY-INGEST", "REQ-TELEMETRY-AUDIT", "REQ-TELEMETRY-BROADCAST"], routeKey: "gemini",
  },
  {
    requirementId: "REQ-TELEMETRY-INTEGRATE", taskId: "TASK-TELEMETRY-INTEGRATE",
    proofId: "PROOF-TELEMETRY-CANDIDATE", clauseId: "AC-REQ-TELEMETRY-INTEGRATE-001",
    title: "Integrate the telemetry service candidate",
    statement: "Join the admitted leaf histories and, in src/app.ts, export async createApp({ auditPath }) returning { audit, hub, handle(request), close() }; GET /stream, not GET /events, must return the live SSE response. The real server entry must keep one long-lived createApp service instance for every HTTP request, pass the inbound GET /stream request to that shared instance so its subscriber is registered, and only then flush headers before awaiting the first SSE frame; do not create a throwaway startup stream or a second per-route service instance. Keep the entry behind an explicit import.meta.url main guard that parses --port/--audit; add project tests.",
    acceptance: "createApp({ auditPath }) returns an import-safe service whose handle(request) passes in-process health, ingest, audit, restart, SSE, and 404 checks and whose close() is safe; GET /stream returns status 200 with an SSE content type and delivers an accepted event, while POST /events remains the ingest route. The guarded executable honors --port and --audit, uses the same long-lived service instance for health, ingest, and stream requests, registers the inbound stream subscriber before calling response.flushHeaders(), flushes before awaiting the first event, proves over real loopback HTTP that POST /events reaches that stream, serves the documented HTTP contract, and passes CLI, documentation, build, and test checks without remote delivery.",
    command: "task verify:candidate",
    positiveOracle: "One joined candidate commit contains every admitted leaf and passes the project rubric.",
    negativeControl: "A partial join or worker-performed remote landing is rejected.",
    ownedPaths: ["src/app.ts", "tests/telemetry.test.ts"],
    dependencyTaskIds: ["TASK-TELEMETRY-CLI"],
    dependencyRequirementIds: ["REQ-TELEMETRY-CLI"],
    routeKey: "minimax",
  },
]);
